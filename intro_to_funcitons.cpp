#include <iostream>
#include <cmath> //pow didn't work as a function at first so i just include cmath and it worked
/* 
    example of a function: function_name(arg1, arg2) 
*/

int main()
{
   int x;
   x = pow(10,2);
   x = x*2; 

   std::cout << x << std::endl;
}